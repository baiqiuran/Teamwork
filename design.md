# 日序前端设计约定

本约定用于同一应用内的页面优化。保留现有 React 页面、业务流程和靛蓝品牌；颜色、字体、间距和控件状态以 `src/style.css` 顶部的设计令牌为准。

## 页面结构

- 工作区保留左侧导航。页面标题只出现一次；说明文字只解释用户做决定所需的范围、后果或下一步。
- 我的日报：编辑区是主区域，最近记录在侧栏；保存状态和提交操作清楚可见。
- 团队日报：保留多列瀑布流，每张卡片完整显示一份日报，突出作者、日期、标题和工作内容。
- 项目与任务：左侧选择项目，右侧依次查看项目资料、任务和工作进展。
- 登录、成员、分享、公开页和 AI 连接沿用相同的标题、表单、卡片和反馈规则。

## 视觉与交互

- 使用既有 `--brand-*`、`--n-*`、语义色、字体与 4/8px 间距令牌；不引入第二套配色或字体。
- 卡片阴影保持轻量，用字号、间距和边界表达层级。主要操作突出，危险操作清楚但不抢占主视觉。
- 桌面验收宽度为 1280–1920px。更窄窗口保持基本可滚动、可操作；不再设计或验收独立手机布局。
- 保留键盘焦点、禁用、加载、错误和成功反馈；动效遵守减少动态效果的系统设置。

## 文案

- 使用简短、具体的操作语言。删除重复的眉题、空泛介绍及对当前控件的复述。
- 日期锁定、草稿与已提交内容的可见范围、公开链接的访问范围、邀请时效和 AI 授权后果必须保留。
- Codex、MCP、Key、命令及配置字段保持准确，不改写为模糊称呼。
