# NestJS 迁移记录

迁移基线为 `4414749`。继续采用 ADR 0001 的单上下文 DDD 模块化单体，保留 SQLite、文件附件、React 和现有 HTTP 契约。

## 运行与验证

已采用 Node 24.15.0、TypeScript 7.0.2、NestJS 12.0.3 与 Express 适配器。使用 TypeScript 编译装饰器与 ESM，生产启动执行编译后的 JavaScript；API 与浏览器测试也加载同一后端产物。

```sh
npm run build
npm start
npm run test:api
npm run test:ui
npm run test:upgrade
npm run test:production
```

开发使用 `npm run dev` 监听后端编译产物；修改前端后仍需构建前端。后端产物位于 `build/server`，前端位于 `dist`。该次迁移保留了 PORT、DAILY_DATABASE_PATH、DAILY_SETUP_KEY 及默认本机监听；后续 ADR 0007 对应的常开创建已取消 DAILY_SETUP_KEY，当前创建与登录步骤以 README 为准。

应用创建变为异步，返回异步 `listen` 与幂等 `close`。每个应用实例有独立的资源所有者，所有仓储共享一条 SQLite 连接，正常关闭与初始化失败均由所有者释放连接。领域和应用类通过工厂 Provider 接入 Nest，不需要框架装饰器。

## 逐项进度

| 任务 | 状态   | 入口                                                                                                                                                         |
| ---- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 01   | 已完成 | 初始化、登录、当前身份、退出；24 项接口/运行检查与 9 项页面回归通过，旧数据升级回退通过                                                                      |
| 02   | 已完成 | 6 项成员邀请接口与 2 项浏览器流程通过；旧邀请、会话及数据升级回退验证通过。                                                                                  |
| 03   | 已完成 | 7 项项目、任务与归档接口验收通过；任务切换页面回归通过；类型、编译和架构检查通过。                                                                           |
| 04   | 已完成 | 12 项接口/运行检查、5 项页面流程与旧数据升级回退通过；补充并修复错误 JSON 返回 400 的回归。                                                                  |
| 05   | 已完成 | 5 项分享、归档及附件接口与 3 项浏览器流程通过；三类旧令牌、附件和升级回退兼容验证通过。                                                                      |
| 06   | 已完成 | 5 项附件、分享和归档接口及 3 项页面流程通过；新增下载响应头、错误 Base64、20 MiB 超限与普通请求限额断言；旧附件升级回退通过。                                |
| 07   | 已完成 | 全部 41 个接口完成切换；干净安装、24 项 API/运行与 9 项页面验收、仅运行依赖启动、旧数据升级回退和架构负例全部通过；双轴审查发现的 1 项兼容问题已修复并复查。 |

全部 41 个原有方法与路径已逐项对照，均由唯一的 Nest Controller 接入。旧 Express 业务路由与服务定位器已删除；Express 仅作为 Nest 适配器、安全中间件及静态页面服务。后端构建先清理专属产物目录，避免删除的路由残留在产物中。

## 升级与回退验证

兼容脚本从 Git 基线取出旧服务，在隔离目录通过 HTTP 创建数据，关闭后交给当前版本读取和写入，再由旧服务读取新数据。覆盖未过期会话、密码、邀请、私人草稿、待重提修改、回执重放、任务历史、三类分享和附件。脚本需要 Git 历史及开发依赖，不接触真实业务数据，结束后删除自己的临时目录。

官方参考：[Nest 12 迁移指南](https://docs.nestjs.com/migration-guide)、[工厂 Provider](https://docs.nestjs.com/fundamentals/custom-providers)、[异常 Filter](https://docs.nestjs.com/exception-filters)。

## 浏览器环境

本机 Edge 在不启动应用的情况下，连续创建并关闭浏览器上下文时第二次新建页面会超时；禁用 GPU 后仍可复现。测试默认仍使用 Edge，可通过 `PLAYWRIGHT_CHANNEL=chromium` 使用与 Playwright 配套的无头 Chromium，先运行 `npx playwright install chromium --only-shell`。也可用 `PLAYWRIGHT_EXECUTABLE_PATH` 指定本机浏览器用于诊断。页面断言未删减，后端迁移不依赖此选项。

## 运行与回退范围

生产目录需要 `build/server/`、`dist/`、`package.json`、`package-lock.json`，在该目录执行 `npm ci --omit=dev` 后使用 `npm start`。启动仍默认监听 127.0.0.1；进程收到停止信号时先关闭监听再关闭数据库。初始化失败和监听失败均释放已经打开的资源，`createApp` 供测试使用的 `close()` 可重复调用。

数据库和附件的位置、停机备份步骤见 README。框架迁移没有新增表、列或更换摘要与快照格式；隔离演练覆盖旧版本读取 Nest 写入的数据。实际回退时先停止新服务，同批保留数据库与附件副本，再使用基线代码及其原启动命令；不要并发运行两版服务写同一数据库。

普通 JSON 上限仍为 4 MiB，附件入口为 28 MiB 的 JSON 解析上限和 20 MiB 的文件上限。超出普通解析上限沿用基线的通用 500/error 响应；本轮未将其改为新的 413 契约。错误 JSON 保留 400，下载保持二进制类型、UTF-8 文件名及 no-store。

所有验证均使用隔离数据及独立进程；本轮不重启正在使用的服务，不修改真实数据库，不进行公网部署。

## 最终验证证据（2026-09-16）

| 验证                             | 结果                                                                                                                           |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 隔离目录 `npm ci` + `npm test`   | 类型、架构、前后端构建通过；24 项 API/运行测试、9 项浏览器测试全部通过，无跳过                                                 |
| 独立运行目录 `npm ci --omit=dev` | Node 直接启动编译产物；页面、初始化、退出/登录、随日报创建任务、提交及匿名公开读取通过；确认无法解析 tsx、TypeScript、Nest CLI |
| 基线数据升级与回退               | `4414749 → Nest → 4414749` 读取及写入验证通过，旧会话、邀请、三类链接、附件、草稿与回执兼容                                    |
| 路由清单                         | 原有 41 个方法/路径与 Controller 一一对应，无重复处理器                                                                        |
| 架构负例                         | 临时加入 Nest 侵入领域/应用、反向依赖、SQL 越界、共享 UI 跨功能依赖及循环导入，均被拒绝；删除后检查通过                        |
| 生命周期                         | 数据库初始化失败、监听失败、多实例隔离、重复关闭、重启与会话有效期验证通过                                                     |
| 页面布局                         | 团队及公开日报桌面卡片布局、筛选、编辑、分享与附件失败恢复保留并通过                                                           |

保留原有 22 项 API 与 9 项浏览器验收，新增编译运行和生命周期两项测试；附件用例中补充响应头、错误 Base64、20 MiB 边界及普通请求上限断言。规格 N01–N04/N15 对应成员、运行及升级检查；N05–N09 对应草稿、项目、提交、任务及查询检查；N10–N14 对应分享、归档和附件检查；N16–N18 对应运行、生命周期、生产依赖及页面检查。

最终浏览器使用与 Playwright 1.63 配套的 Chromium Headless Shell 153.0.8010.12，未删减原有断言。首次干净安装后的运行测试触及原 15 秒启动等待边界；将测试等待上限改为 60 秒后，完整运行测试约 15.2 秒完成。此设置仅影响验证脚本，没有改变应用启动或业务超时。

npm 12 在从 npm run 内部执行安装时，会把用户配置的 allow-scripts 作为环境变量传递并拒绝项目安装；验证脚本只移除这一继承变量，让隔离安装重新读取原配置文件。没有修改全局 npm 配置或扩大安装脚本权限。本机 esbuild 安装脚本被原配置拦截，但干净构建与全部验收仍通过。

## 双轴审查

### Standards

独立审查对照 AGENTS、CONTEXT、架构文档、ADR 0001 与 README，未发现需修改的规范问题或达到建议修改程度的新增 code smell。Nest 委托与工厂装配符合既定分层。

### Spec

独立审查发现 1 项 P2：静态页面回退对 API 前缀的判断区分大小写，偏离 Express 原有匹配语义。已在 `e47a764` 修正，并在有构建、无构建两种环境添加真实 HTTP 回归：修复前分别返回 HTML 和 503，修复后已知接口返回 JSON、未知接口返回 JSON 404。专项两项测试通过，修复后的干净安装、完整 24 项接口/运行与 9 项页面测试、独立运行依赖验证全部通过；规格审查已只读复查确认解决，无新增发现。

审查统计：Standards 0 项；Spec 1 项 P2 已修复，未解决项 0。全部 7 张本地任务的 Progress 已更新为 done。
